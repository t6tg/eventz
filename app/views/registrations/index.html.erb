<h1>Registration for <%= link_to @event.name, event_path(@event)%> </h1>
<u class="registrations">
  <% @registrations.each do |r| %>
    <li>
      <%= link_to r.user.name, r.user %> &bull; <%= r.how_heard %>
    </li>
  <% end %>
</ul>
